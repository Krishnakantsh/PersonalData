RewriteEngine On
RewriteBase /

# Force non-www (optional)
RewriteCond %{HTTP_HOST} ^www\.ktrsofttech\.com$ [NC]
RewriteRule ^(.*)$ https://ktrsofttech.com/$1 [L,R=301]

# Redirect all requests to public directory
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ public/$1 [L,L]

# Security: disable access to .env, .htaccess, etc.
<FilesMatch "^\.">
  Require all denied
</FilesMatch>

<IfModule mod_dir.c>
  DirectoryIndex index.php
</IfModule>
